<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <!-- The two drivers, and NOTHING else. See DatabaseConnectors' class doc for why ONE project
         serves both dialects, and what that costs a Postgres-only deployment. -->
    <PackageReference Include="Npgsql" Version="10.0.3" />
    <PackageReference Include="Microsoft.Data.SqlClient" Version="7.0.2" />
  </ItemGroup>
  <ItemGroup>
    <!-- Runtime-agnostic by construction: Contracts + AppCore only, exactly what StreamsForge.AppCore
         itself depends on. No Orleans, no Dapr, no ASP.NET — both hosts reference this one project. -->
    <ProjectReference Include="..\StreamsForge.AppCore\StreamsForge.AppCore.csproj" />
    <ProjectReference Include="..\StreamsForge.Contracts\StreamsForge.Contracts.csproj" />
  </ItemGroup>
</Project>
