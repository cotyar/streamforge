<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <!-- The FIX session engine, and NOTHING else — see FixInboundTransport's class doc for why
         QuickFIXn.Core is a dependency of this project alone; StreamForge.AppCore gains nothing. -->
    <PackageReference Include="QuickFIXn.Core" Version="1.14.1" />
  </ItemGroup>
  <ItemGroup>
    <!-- Runtime-agnostic by construction: Contracts + AppCore only, exactly what
         StreamForge.Connectors.Database references. No Orleans, no Dapr, no ASP.NET — both hosts
         reference this one project (wave D). -->
    <ProjectReference Include="..\StreamForge.AppCore\StreamForge.AppCore.csproj" />
    <ProjectReference Include="..\StreamForge.Contracts\StreamForge.Contracts.csproj" />
  </ItemGroup>
</Project>
