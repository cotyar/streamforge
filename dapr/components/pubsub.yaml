apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: pubsub
spec:
  type: pubsub.redis
  version: v1
  metadata:
    - name: redisHost
      value: localhost:6379
    - name: redisPassword
      value: ""
# Deliberately UNSCOPED (W8): the polyglot premise is that any local sidecar'd process — the host
# (streamsforge-dapr), the python enricher (sf-enricher), the ts/java consumers — publishes and
# subscribes through this same component. Topics: sf-sources, sf-pipeline-out, sf-table-delta,
# sf-lifecycle, sf-metrics + per-source egress sf-source-{name}. Contract: dapr/POLYGLOT.md.
