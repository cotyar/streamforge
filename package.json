{
  "name": "streamforge",
  "private": true,
  "description": "Workspace root. Exists so the local packages link instead of copy — see the comment in AGENTS.md's build section.",
  "workspaces": [
    "web",
    "clients/typescript",
    "clients/tanstack-db",
    "clients/react"
  ],
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "build": "bun run --cwd web build",
    "test": "bun run --cwd clients/typescript test && bun run --cwd clients/tanstack-db test && bun run --cwd clients/react test && bun test web/test admin/"
  }
}
